import { Dealer } from '../dealer';

// TOD0 - write a test to ensure dealer behaviors properly
const fakePlayerState = {};
it('should have a dealer that who holds when below 17', () => {
  const dealer = new Dealer();
});
